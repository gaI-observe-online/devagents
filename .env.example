## GADOS local configuration (safe defaults)
#
# Copy to `.env` and edit as needed:
#   cp .env.example .env
#
# Notes:
# - By default, OTel export is disabled so `make up` works without Docker/Grafana.
# - Write endpoints are **anonymous** unless basic auth is configured.

## Runtime storage (local)
GADOS_RUNTIME_DIR=.gados-runtime

## Audit logs (optional override; defaults to gados-project/log/bus/bus-events.jsonl)
# GADOS_AUDIT_DIR=.gados-runtime/audit

## Control-plane auth (optional)
# If BOTH are set, write endpoints require HTTP Basic auth.
# GADOS_BASIC_AUTH_USER=admin
# GADOS_BASIC_AUTH_PASSWORD=change-me

## Security knobs
# Comma-separated origins. Empty => no CORS headers emitted.
# GADOS_CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:8000
GADOS_MAX_REQUEST_BYTES=1048576
GADOS_RATE_LIMIT_RPS=10
GADOS_RATE_LIMIT_BURST=20

## Observability (OTel)
# Keep disabled by default so local run works without LGTM.
OTEL_SDK_DISABLED=1
# If you enable OTel, set:
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic <token>
# OTEL_SERVICE_NAME=gados-control-plane
# DEPLOYMENT_ENV=local
#
# Trace sampling (recommended defaults):
# - Local: sample everything.
# - Grafana Cloud free: start at 0.25 to reduce volume.
# GADOS_TRACE_SAMPLE_RATIO=1.0
# Or use standard OTel sampler vars:
# OTEL_TRACES_SAMPLER=parentbased_traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.25

## Analytics scrubbing (PII/secret safety)
# Only these property keys are allowed through to logs/traces.
# Everything else is dropped, and suspicious keys/values are redacted.
# GADOS_ANALYTICS_PROPERTIES_ALLOWLIST=scenario,decision,run_id,run_key,story_id,epic_id,status,severity,component,route,method,http_status,correlation_id

## Notifications
# Optional realtime webhook dispatch (queue+digest always works locally).
# GADOS_WEBHOOK_URL=https://example.com/webhook
# GADOS_WEBHOOK_HMAC_SECRET=change-me
GADOS_WEBHOOK_MIN_SEVERITY=CRITICAL

